# QUACK Phase 5 — Production Runtime + Multi-Process Recovery

Saved: 2026-09-08 (Phase 5 finalization session)

## Release-gate state

```text
Phase 5 implementation:         COMPLETE (5A–5L; 5M local commit)
Phase 5 publication (5N):       BLOCKED — no git remote, no GitHub auth
Local release gate:             PASS (2026-09-08)
Release decision:               PRODUCTION CANDIDATE (Windows-verified;
                                 Linux/macOS/ARM64 CI never executed)
Minimum user action to unblock: create GitHub repo →
                                git remote add origin <url> →
                                git push -u origin <branch> →
                                observe CI matrix → record evidence
```

## Verified state (final run 2026-09-08, includes ALL changes)

- Full suite: 1,456 ordinary + 17 serial, 0 failures/skips/cancellations
- typecheck PASS · SDK typecheck PASS · lint PASS · build PASS
- Focused: platform+terminal+git-status 20 · skills/privacy/sandbox 30 ·
  isolation 17 · coordination 9 · multi-process recovery 12 (real forks) ·
  interrupted-recovery 22 · adversarial cross-suite 22 · compiler 9 ·
  SDK 3

## Phase 5 what-shipped (quick map for the next session)

- `src/runtime/ownership.ts` — MissionOwnershipGuard: lifecycle
  UNOWNED→ACQUIRING→OWNED→HEARTBEATING→COMPLETING→RELEASED +
  OWNERSHIP_CONFLICT/LEASE_LOST/STALE_OWNER; atomic acquire;
  heartbeat lease/3 (unref'd); release tombstone; assertOwnedForWrite
  fencing (owner+epoch+live-lease check before every durable write).
- `src/runtime/runtime.ts` — resumeMission + fresh durable submitGoal
  acquire mission leases before durable transitions; fencedTransition +
  assertOwnedForWrite wrap checkpoint init, mission-state transitions,
  task stores, receipt/SUCCEEDED/completed persists; failure transitions
  also fenced; release AFTER final persist; `disableOwnership` flag;
  deps `coordinationDbPath`/`ownershipLeaseMs`; dataDir auto-derives
  `<dataDir>/coordination.sqlite`.
- `src/events/event-bus.ts` — ownership.* and mission.* typed events.
- `src/runtime/multi-process-recovery.test.ts` (12) +
  `src/test-support/{ownership-child,multi-process-recovery-types}.ts` —
  real-fork crash harness + dogfood E2E.
- `docs/recovery/multi-process-recovery.md` (SUPPORTED, full model) ·
  `docs/recovery/durable-state.md` (5E; NO auto retention) ·
  `docs/operations/README.md` (deployment, lease tuning, runbook) ·
  ADR 0031 status update (boundary lifted; history preserved).

## Key gotchas (accumulated — preserve)

- atomicWriteFile(path, STRING) — JSON.stringify first
- Windows 8.3 short paths: realpath BOTH sides for containment compares
- worker_threads share process.env; ENFORCED materialization ≠ sealing
- Registry/skill IDs must be dotted (extension.invalid_id otherwise)
- node:test + assert/strict; no vitest; PowerShell Select-String/Select-Object
- Terminal tool test: write probe .js file, never inline -e quoting
- Full suite ~10min: focused first, full once per stage
- swe-system QuackSystem type has MANY required fields — read interface first
- CURRENT_STATE.md layout: phase tables → verification table (newest
  first) → continuation sections (newest first) → Next priority near end
- spawn("npx", shell:false) ENOENT on Windows — use resolveNodeCliArgv
- static-quality.mjs guards: exempt scripts/, *.test.*,
  platform/process.ts, cli.ts, tools/terminal.ts; comment wording must
  avoid literal "2>&1"/"|| true"
- node:sqlite run() returns { changes }; SkillRegistryStore contract is
  SYNC — never fire async writes in save()
- discovery manifest field is `id` (not skillId) inside skill.json
- SafeSkillCompiler extractToolIds only matches core|tool|plugin|workspace|admin.
  prefixed tool ids — test tools need that prefix for SKILL.md resolution
- Cross-process retry-safe replay needs SAME tool id + SAME retrySafety
  registered in the resuming process + grant scope covering the tool
- Crashed owner's live lease blocks takeover until expiry — tests must
  wait leaseMs+ before second-process resume (multi-process suite: 600ms
  lease, 900ms wait)
- createQuackSystem tests: await system.events.drain() before rm(dataDir)
  (async audit sink races cleanup → ENOTEMPTY)
- Fork child argv parsing: extra JSON is the LAST argument (crashPoint
  optional positional between mode and JSON)

## Next session

Phase 5 is closed. Do NOT start Phase 6 unprompted. If the user provides
a git remote: add remote, push, watch CI matrix, record per-OS evidence
in docs/platform/, re-evaluate PRODUCTION READY. Future-capability list
stays in CURRENT_STATE.md "Next priority".
