/** Typed contract used by the embedded, dependency-free QUACK Control Room client. */
export type StudioRoute =
  | "overview" | "console" | "missions" | "new-mission" | "mission" | "agents" | "models"
  | "actions" | "integrations" | "browser" | "files" | "approvals" | "traces" | "trace" | "artifacts" | "evidence" | "audit"
  | "ecosystem" | "system" | "settings";

export interface StudioApiClientContract {
  readonly dashboardState: "/dashboard/state";
  readonly missions: "/missions";
  readonly proposals: "/improvement/proposals";
  readonly providers: "/providers";
  readonly actions: "/actions";
  readonly mcp: "/mcp";
  readonly agents: "/agents";
  readonly memory: "/memory";
  readonly extensions: "/extensions";
  readonly settings: "/settings";
  readonly systemStatus: "/system/status";
  readonly approvals: "/approvals";
  readonly events: "/events";
}

export const STUDIO_API: StudioApiClientContract = {
  dashboardState: "/dashboard/state",
  missions: "/missions",
  proposals: "/improvement/proposals",
  providers: "/providers",
  actions: "/actions",
  mcp: "/mcp",
  agents: "/agents",
  memory: "/memory",
  extensions: "/extensions",
  settings: "/settings",
  systemStatus: "/system/status",
  approvals: "/approvals",
  events: "/events",
};

export function studioHtml(): string {
  return `<!doctype html>
<html lang="en" data-theme="dark">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="color-scheme" content="dark light">
  <title>QUACK Control Room</title>
  <link rel="icon" href="data:,">
  <link rel="stylesheet" href="/dashboard/styles.css">
</head>
<body>
  <a class="skip-link" href="#studio-page">Skip to content</a>
  <div class="studio-shell">
    <aside class="sidebar" aria-label="Primary navigation">
      <a class="brand" href="#overview" aria-label="QUACK Control Room home"><span class="brand-mark" aria-hidden="true">Q</span><span><strong>QUACK</strong><small>Control Room</small></span></a>
      <nav class="nav" id="studio-nav">
        <p class="nav-group">Operate</p>
        <a href="#overview" data-route="overview">Overview</a>
        <a href="#console" data-route="console">Console</a>
        <a href="#missions" data-route="missions">Missions</a>
        <a href="#agents" data-route="agents">Agents</a>
        <a href="#models" data-route="models">Models</a>
        <p class="nav-group">Act</p>
        <a href="#actions" data-route="actions">Actions</a>
        <a href="#integrations" data-route="integrations">Integrations</a>
        <a href="#browser" data-route="browser">Browser</a>
        <a href="#files" data-route="files">Files</a>
        <p class="nav-group">Govern</p>
        <a href="#approvals" data-route="approvals">Approvals <span class="nav-count" id="approval-count">0</span></a>
        <a href="#traces" data-route="traces">Traces</a>
        <a href="#artifacts" data-route="artifacts">Artifacts</a>
        <a href="#evidence" data-route="evidence">Evidence</a>
        <a href="#ecosystem" data-route="ecosystem">Ecosystem</a>
        <a href="#audit" data-route="audit">Audit</a>
        <a href="#system" data-route="system">System</a>
        <a href="#settings" data-route="settings">Settings</a>
      </nav>
      <div class="sidebar-foot"><span class="status-dot" id="runtime-dot"></span><span id="runtime-label">Connecting</span></div>
    </aside>
    <main class="studio-main">
      <header class="studio-topbar">
        <div><p class="eyebrow" id="page-kicker">SOLO PRODUCTION</p><h1 id="page-title" tabindex="-1">Overview</h1><p class="page-summary" id="page-summary">Operational truth from this local runtime.</p></div>
        <div class="top-actions"><button class="icon-button" id="theme-toggle" type="button" aria-label="Switch color theme">Theme</button><a class="button" href="#new-mission">New mission</a></div>
      </header>
      <section id="studio-alert" class="alert" role="status" hidden></section>
      <section id="studio-page" class="studio-page" aria-live="polite" tabindex="-1"><p class="empty">Loading Control Room...</p></section>
    </main>
  </div>
  <div class="modal-backdrop" id="setup-wizard" hidden>
    <section class="modal" role="dialog" aria-modal="true" aria-labelledby="setup-title">
      <p class="eyebrow">FIRST RUN</p><h2 id="setup-title">Verify this machine before your first mission</h2>
      <p class="muted">The checks below report setup state. They do not send prompts, install software, or expose credentials.</p>
      <div id="setup-checks" class="check-list"><p class="empty">Running local checks...</p></div>
      <div class="actions"><button class="button" id="setup-finish" type="button">Continue to Control Room</button></div>
    </section>
  </div>
  <script src="/dashboard/app.js"></script>
</body>
</html>`;
}

export function studioStyles(): string {
  return `:root{color-scheme:dark;--bg:#0a0d10;--surface:#11161b;--surface-2:#171d23;--line:#29323a;--line-strong:#3a4650;--text:#f3f5f6;--muted:#9da8b1;--quiet:#717d86;--accent:#e7b64a;--accent-ink:#1b1405;--good:#59c69b;--warn:#e7b64a;--bad:#ee7d75;--focus:#7cc4ff;--radius:6px;font-family:"Segoe UI Variable Text","Segoe UI",ui-sans-serif,system-ui,sans-serif;background:var(--bg);color:var(--text).lane{border:1px solid var(--line);border-radius:var(--radius);padding:12px;background:var(--surface-2)}.lane h3{margin:0 0 10px;font-size:13px;letter-spacing:.06em;text-transform:uppercase;color:var(--muted)}.lane .nav-count{margin-left:4px}.lane-item{display:flex;flex-direction:column;gap:4px;padding:9px 10px;border:1px solid var(--line);border-radius:4px;background:var(--surface);color:var(--text);text-decoration:none;margin-bottom:6px}.lane-item:hover,.lane-item:focus-visible{border-color:var(--accent)}.lane-item strong{font-size:13px;line-height:1.3}.lane-item span{display:flex;align-items:center;gap:8px;font-size:12px}.mono{font-family:ui-monospace,Consolas,monospace;font-size:12px;word-break:break-all}.console-panel{display:grid;gap:12px}.console-log{display:flex;flex-direction:column;gap:8px;max-height:52vh;overflow-y:auto;border:1px solid var(--line);border-radius:var(--radius);padding:12px;background:var(--surface-2)}.console-turn{border:1px solid var(--line);border-radius:4px;padding:9px 11px;line-height:1.45;background:var(--surface)}.console-turn.operator{border-color:var(--accent)}.console-turn.operator.error{border-color:var(--bad)}.console-turn.system{color:var(--muted)}.console-meta{font-size:11px;color:var(--quiet);margin-bottom:3px;letter-spacing:.04em;text-transform:uppercase}.console-form{display:flex;gap:8px}.console-form input{flex:1;min-width:0;padding:9px 11px;border:1px solid var(--line-strong);border-radius:var(--radius);background:var(--surface);color:var(--text)}.console-turn.stream{border-color:var(--focus);color:var(--text)}.trace-filters{display:flex;gap:10px;align-items:end}.trace-item{display:grid;gap:4px;padding:8px 10px;border:1px solid var(--line);border-radius:4px;background:var(--surface);margin-bottom:6px}.trace-item .badge{justify-self:start}}html[data-theme="light"]{color-scheme:light;--bg:#f3f1ec;--surface:#fffefa;--surface-2:#ebe8e1;--line:#d1ccc1;--line-strong:#8e887e;--text:#181b1e;--muted:#505860;--quiet:#4b535a;--accent:#795000;--accent-ink:#fff;--good:#126d4d;--warn:#795000;--bad:#a9342e;--focus:#1269a6}*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;min-width:320px;background:var(--bg);color:var(--text)}button,input,select,textarea{font:inherit}.skip-link{position:fixed;z-index:20;left:12px;top:8px;transform:translateY(-150%);background:var(--text);color:var(--bg);padding:10px 14px}.skip-link:focus{transform:none}.studio-shell{min-height:100vh;display:grid;grid-template-columns:224px minmax(0,1fr)}.sidebar{position:sticky;top:0;height:100vh;padding:18px 12px;border-right:1px solid var(--line);background:var(--surface);display:flex;flex-direction:column}.brand{display:flex;align-items:center;gap:10px;padding:3px 7px 20px;color:var(--text);text-decoration:none}.brand-mark{display:grid;place-items:center;width:32px;height:32px;border:1px solid var(--accent);border-radius:4px;color:var(--accent);font-weight:800}.brand span:last-child{display:grid;line-height:1.1}.brand strong{font-size:15px;letter-spacing:.08em}.brand small{margin-top:3px;color:var(--muted);font-size:11px}.nav{display:grid;gap:2px}.nav-group{margin:15px 9px 5px;color:var(--quiet);font-size:10px;font-weight:700;letter-spacing:.14em;text-transform:uppercase}.nav a{display:flex;align-items:center;justify-content:space-between;min-height:38px;padding:8px 10px;border-left:2px solid transparent;color:var(--muted);text-decoration:none;font-size:13px}.nav a:hover{background:var(--surface-2);color:var(--text)}.nav a.active{border-left-color:var(--accent);background:var(--surface-2);color:var(--text);font-weight:650}.nav-count{min-width:20px;padding:1px 5px;border:1px solid var(--line-strong);border-radius:999px;text-align:center;font-size:10px}.sidebar-foot{display:flex;align-items:center;gap:8px;margin-top:auto;padding:14px 9px 4px;border-top:1px solid var(--line);color:var(--muted);font-size:11px}.status-dot{width:7px;height:7px;border-radius:50%;background:var(--warn)}.status-dot.ok{background:var(--good)}.status-dot.bad{background:var(--bad)}.studio-main{min-width:0;padding:0 30px 44px}.studio-topbar{display:flex;justify-content:space-between;align-items:flex-start;gap:24px;padding:28px 0 22px;border-bottom:1px solid var(--line)}.eyebrow{margin:0 0 6px;color:var(--accent);font-size:10px;font-weight:750;letter-spacing:.15em}.studio-topbar h1{margin:0;font-size:27px;line-height:1.15;letter-spacing:-.02em}.page-summary{margin:7px 0 0;color:var(--muted);font-size:13px}.top-actions,.actions{display:flex;flex-wrap:wrap;gap:8px;align-items:center}.studio-page{padding-top:22px;outline:none}.grid{display:grid;gap:14px}.grid.cards{grid-template-columns:repeat(4,minmax(0,1fr))}.grid.two{grid-template-columns:repeat(2,minmax(0,1fr))}.card,.panel{border:1px solid var(--line);border-radius:var(--radius);background:var(--surface)}.card{min-height:112px;padding:16px}.card-label{color:var(--muted);font-size:11px;font-weight:650;text-transform:uppercase;letter-spacing:.06em}.card-value{margin-top:14px;font-size:25px;font-weight:720;letter-spacing:-.02em}.card-note{margin-top:7px;color:var(--quiet);font-size:11px}.panel{padding:18px}.panel h2,.panel h3{margin:0 0 12px;font-size:15px}.panel-header{display:flex;align-items:flex-start;justify-content:space-between;gap:12px;margin-bottom:12px}.muted,.empty{color:var(--muted);font-size:13px;line-height:1.55}.empty{padding:12px 0}.table-wrap{overflow:auto;border-top:1px solid var(--line)}.table{width:100%;border-collapse:collapse;font-size:12px}.table th{text-align:left;color:var(--quiet);font-weight:700;padding:10px 9px;border-bottom:1px solid var(--line);text-transform:uppercase;letter-spacing:.05em;font-size:10px}.table td{padding:11px 9px;border-bottom:1px solid var(--line);vertical-align:top}.table tr:last-child td{border-bottom:0}.table a{color:var(--text);text-decoration-color:var(--accent);text-underline-offset:3px}.badge{display:inline-flex;align-items:center;gap:5px;border:1px solid var(--line-strong);border-radius:999px;padding:3px 7px;color:var(--muted);font-size:10px;font-weight:750;text-transform:uppercase;letter-spacing:.03em}.badge.ok{border-color:color-mix(in srgb,var(--good) 65%,var(--line));color:var(--good)}.badge.warn,.badge.risk{border-color:color-mix(in srgb,var(--warn) 65%,var(--line));color:var(--warn)}.badge.danger{border-color:color-mix(in srgb,var(--bad) 65%,var(--line));color:var(--bad)}.form-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px}.field{display:grid;gap:6px}.field.full{grid-column:1/-1}.field label{font-size:11px;color:var(--muted);font-weight:650}.field input,.field select,.field textarea,.search-row input{width:100%;border:1px solid var(--line-strong);border-radius:4px;background:var(--bg);color:var(--text);padding:10px}.field textarea{min-height:132px;resize:vertical}.checkbox{display:flex;align-items:center;gap:8px;color:var(--muted);font-size:13px}.actions{margin-top:16px}.button,.icon-button{min-height:38px;border:1px solid var(--accent);border-radius:4px;padding:8px 12px;background:var(--accent);color:var(--accent-ink);font-weight:700;text-decoration:none;cursor:pointer}.button.secondary,.icon-button{border-color:var(--line-strong);background:transparent;color:var(--text)}.button.danger{border-color:var(--bad);background:transparent;color:var(--bad)}.button:hover,.icon-button:hover{filter:brightness(1.1)}.button:disabled{opacity:.5;cursor:not-allowed}:focus-visible{outline:3px solid var(--focus);outline-offset:2px}.alert{margin-top:16px;padding:11px 13px;border-left:3px solid var(--focus);background:var(--surface-2);color:var(--text);font-size:13px}.alert.error{border-color:var(--bad)}.detail-list,.check-list{display:grid;gap:0}.detail-row,.check-row{display:grid;grid-template-columns:150px minmax(0,1fr);gap:12px;padding:9px 0;border-bottom:1px solid var(--line);font-size:12px}.detail-row:last-child,.check-row:last-child{border-bottom:0}.detail-row strong,.check-row strong{color:var(--muted)}.callout{border-left:3px solid var(--warn);padding:10px 12px;background:var(--surface-2);color:var(--muted);font-size:12px;line-height:1.5}.section-gap{margin-top:14px}.modal-backdrop{position:fixed;z-index:10;inset:0;display:grid;place-items:center;padding:20px;background:rgba(0,0,0,.68)}.modal-backdrop[hidden]{display:none}.modal{width:min(590px,100%);max-height:90vh;overflow:auto;border:1px solid var(--line-strong);border-radius:var(--radius);background:var(--surface);padding:24px}.modal h2{margin:0;font-size:22px}.search-row{display:flex;gap:8px}.mono{font-family:"Cascadia Mono",Consolas,monospace;font-size:11px;word-break:break-all}@media(max-width:1050px){.grid.cards{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:820px){.studio-shell{grid-template-columns:1fr}.sidebar{position:static;height:auto;border-right:0;border-bottom:1px solid var(--line)}.brand{padding-bottom:10px}.nav{display:flex;overflow:auto;padding-bottom:4px}.nav-group,.sidebar-foot{display:none}.nav a{white-space:nowrap;border-left:0;border-bottom:2px solid transparent}.nav a.active{border-left:0;border-bottom-color:var(--accent)}.studio-main{padding:0 16px 28px}.studio-topbar{padding:20px 0}.grid.two,.form-grid{grid-template-columns:1fr}.field.full{grid-column:auto}}@media(max-width:520px){.grid.cards{grid-template-columns:1fr}.studio-topbar{flex-direction:column}.top-actions{width:100%}.top-actions .button{flex:1;text-align:center}.detail-row,.check-row{grid-template-columns:1fr;gap:3px}.modal{padding:18px}}@media(prefers-reduced-motion:reduce){html{scroll-behavior:auto}*,*:before,*:after{animation-duration:.01ms!important;transition-duration:.01ms!important}}`;
}

export function studioScript(): string {
  return `"use strict";
const api={dashboardState:"/dashboard/state",missions:"/missions",proposals:"/improvement/proposals",providers:"/providers",actions:"/actions",mcp:"/mcp",agents:"/agents",memory:"/memory",extensions:"/extensions",settings:"/settings",systemStatus:"/system/status",approvals:"/approvals",events:"/events",modelsStream:"/models/stream",audit:"/audit"};
const routes={overview:["Overview","Operational truth from this local runtime."],console:["Console","Conversational mission control. Compose a mission, watch its real state."],missions:["Missions","Plan, run, inspect, and verify bounded work."],"new-mission":["New mission","Define an outcome and safety posture before execution."],mission:["Mission detail","Trace plan, events, evidence, and final state."],agents:["Agents","Registered workers and their current runtime state."],models:["Models","Provider capability, health, and certification evidence."],actions:["Actions","Discoverable capabilities and durable execution history."],integrations:["Integrations","MCP servers, transports, isolation, and health."],browser:["Browser","Policy-gated browser automation and artifact boundaries."],files:["Files","Workspace access boundaries and file-action controls."],approvals:["Approvals","Human decisions waiting on this operator."],traces:["Trace Center","Mission timelines from the durable trace repository."],trace:["Trace detail","Execution timeline, capabilities, evidence, verification, receipt."],artifacts:["Artifacts","Evidence- and workspace-backed outputs. No second store."],evidence:["Evidence","Mission, decision, failure, evaluation, and audit records."],ecosystem:["Ecosystem","Governed extension catalog: manifest, integrity, lifecycle — metadata only."],audit:["Audit","Security and governance record — distinct from traces."],system:["System","Machine readiness and security posture."],settings:["Settings","Safe runtime controls that cannot weaken hard gates."]};
const $=id=>document.getElementById(id);const page=$("studio-page");let currentRoute="overview";
const esc=value=>String(value??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
const fmt=value=>value?new Date(value).toLocaleString():"—";
const tone=value=>/AVAILABLE|COMPLETED|APPROVED|SUCCEEDED|HEALTHY|active|healthy|success|configured/i.test(String(value))?"ok":/FAILED|REJECTED|OFFLINE|UNAVAILABLE|UNKNOWN_EXTERNAL|error/i.test(String(value))?"danger":/NEEDS_SETUP|AWAITING|PROPOSED|QUEUED|RUNNING|VERIFYING|RECONCILING|REQUIRES/i.test(String(value))?"warn":"";
const badge=value=>'<span class="badge '+tone(value)+'">'+esc(value??"unknown")+'</span>';
function notice(message,error=false){const box=$("studio-alert");box.hidden=!message;box.textContent=message||"";box.className="alert"+(error?" error":"");}
async function request(url,options={}){const response=await fetch(url,{headers:{"Content-Type":"application/json","X-QUACK-CSRF":"1",...(options.headers||{})},...options});const body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(body.error?.message||("Request failed ("+response.status+")"));return body;}
function route(){const raw=location.hash.replace(/^#/,"")||"overview";const [name,id]=raw.split("/");currentRoute=routes[name]?name:"overview";$("page-title").textContent=routes[currentRoute][0];$("page-summary").textContent=routes[currentRoute][1];document.querySelectorAll(".nav a").forEach(a=>a.classList.toggle("active",a.dataset.route===currentRoute));notice("");render(id).catch(error=>{notice(error.message,true);page.innerHTML='<p class="empty">This surface could not load. The runtime did not report a successful state.</p>';});}
async function render(id){if(currentRoute==="overview")return renderOverview();if(currentRoute==="console")return renderConsole();if(currentRoute==="missions")return renderMissions();if(currentRoute==="new-mission")return renderNewMission();if(currentRoute==="mission")return renderMission(id);if(currentRoute==="agents")return renderAgents();if(currentRoute==="models")return renderModels();if(currentRoute==="actions")return renderActions();if(currentRoute==="integrations")return renderIntegrations();if(currentRoute==="browser")return renderBrowser();if(currentRoute==="files")return renderFiles();if(currentRoute==="approvals")return renderApprovals();if(currentRoute==="traces")return renderTraces();if(currentRoute==="trace")return renderTraceDetail(id);if(currentRoute==="artifacts")return renderArtifacts();if(currentRoute==="evidence")return renderEvidence();if(currentRoute==="ecosystem")return renderEcosystem();if(currentRoute==="audit")return renderAudit();if(currentRoute==="system")return renderSystem();return renderSettings();}
function card(label,value,note=""){return '<article class="card"><div class="card-label">'+esc(label)+'</div><div class="card-value">'+(typeof value==="string"&&tone(value)?badge(value):esc(value))+'</div>'+(note?'<div class="card-note">'+esc(note)+'</div>':'')+'</article>';}
function row(label,value,raw=false){return '<div class="detail-row"><strong>'+esc(label)+'</strong><span>'+(raw?String(value):esc(value??"—"))+'</span></div>';}
function table(headers,rows){return rows.length?'<div class="table-wrap"><table class="table"><thead><tr>'+headers.map(h=>'<th scope="col">'+esc(h)+'</th>').join("")+'</tr></thead><tbody>'+rows.join("")+'</tbody></table></div>':'<p class="empty">No records reported.</p>';}
async function renderOverview(){const [state,providers,actions,status]=await Promise.all([request(api.dashboardState),request(api.providers),request(api.actions),request(api.systemStatus)]);const missions=state.missions||{active:[],completed:[],failed:[],all:[]};const available=providers.providers.filter(p=>p.available).length;page.innerHTML='<div class="grid cards">'+card("Runtime",status.runtime.status||"HEALTHY","Loopback authenticated")+card("Active missions",missions.active.length,"Durable mission state")+card("Providers",available+" / "+providers.providers.length,"Currently reachable")+card("Waiting approvals",status.actions.awaitingApproval,"No automatic consent")+'</div><div class="grid two section-gap"><section class="panel"><div class="panel-header"><div><h2>Recent missions</h2><p class="muted">Last known durable state</p></div><a class="button secondary" href="#missions">View all</a></div>'+missionTable((missions.all||[]).slice(0,6))+'</section><section class="panel"><h2>Readiness blockers</h2>'+row("NVIDIA NIM",badge(status.setup.nvidia),true)+row("Ollama",badge(status.setup.ollama),true)+row("Browser runtime",badge(status.setup.browser),true)+row("Ambiguous actions",status.actions.ambiguous)+row("MCP configured",status.mcp.configured)+'</section></div><section class="panel section-gap"><h2>Safety posture</h2><p class="callout">Outbound network is default-deny. Writes are ledgered before execution. Approval-gated actions cannot run from this screen without an explicit decision.</p></section>';$("approval-count").textContent=String(status.actions.awaitingApproval);}
function missionTable(missions){return table(["Goal","State","Updated"],missions.map(m=>'<tr><td><a href="#mission/'+encodeURIComponent(m.id)+'">'+esc(m.goal)+'</a></td><td>'+badge(m.state)+'</td><td>'+esc(fmt(m.updatedAt))+'</td></tr>'));}
const consoleState={turns:[],tracked:null,connected:false};
function consoleTurn(kind,text,extra="") {consoleState.turns.push({kind,text,extra,at:new Date().toISOString()});if(consoleState.turns.length>80)consoleState.turns.shift();}function consoleDom(){return '<section class="panel console-panel"><div class="panel-header"><div><h2>Conversation</h2><p class="muted">Compose a mission and watch its real state. The Console is a client of QUACK Core — it never executes anything itself.</p></div><span class="badge '+(consoleState.connected?"ok":"warn")+'" id="console-stream-badge">'+(consoleState.connected?"STREAM LIVE":"STREAM OFFLINE")+'</span></div><div class="console-log" id="console-log" aria-live="polite">'+(consoleState.turns.length?consoleState.turns.map(turn=>consoleTurnHtml(turn)).join(""):'<p class="empty">No conversation yet. Submit an outcome below to start a mission.</p>')+'</div><form id="console-form" class="console-form"><label class="skip-link" for="console-input">Mission outcome</label><input id="console-input" autocomplete="off" placeholder="Describe an outcome for the runtime to plan and verify…" required><button class="button" type="submit" id="console-submit">Run mission</button><button class="button secondary" type="button" id="console-ask">Ask model</button></form><div id="console-hint" class="muted" '+(consoleState.connected?"":"hidden")+'><p class="callout">Live stream lost. Events below may be incomplete — open <a href="#missions">Missions</a> or the mission detail trace for the full durable history. The stream reconnects automatically.</p></div></section>';}
function consoleTurnHtml(turn){const kind=turn.kind==="operator"?"console-turn operator":turn.kind==="error"?"console-turn operator error":turn.kind==="stream"?"console-turn stream":"console-turn system";return '<article class="'+kind+'"><div class="console-meta">'+esc(turn.kind==="operator"?"operator":turn.kind==="stream"?"governed model":"runtime")+' · '+esc(fmt(turn.at))+'</div><div>'+(turn.text)+(turn.extra?'<div class="muted">'+turn.extra+'</div>':'')+'</div></article>';}
async function renderConsole(){page.innerHTML=consoleDom();$("console-form").addEventListener("submit",submitConsoleMission);$("console-ask").addEventListener("click",askConsoleModel);consoleScrollDown();}
async function askConsoleModel(){const input=$("console-input");const goal=input.value.trim();if(!goal)return;input.value="";consoleTurn("operator","<strong>"+esc(goal)+"</strong>");const streamTurn={kind:"stream",text:"",at:new Date().toISOString()};consoleState.turns.push(streamTurn);refreshConsoleLog();try{const response=await fetch(api.modelsStream,{method:"POST",headers:{"Content-Type":"application/json","X-QUACK-CSRF":"1"},body:JSON.stringify({prompt:goal,actor:"console-user"})});if(!response.ok||!response.body){const body=await response.json().catch(()=>({}));streamTurn.text="<strong>Governed stream rejected:</strong> "+esc(body.error?.message||("HTTP "+response.status));refreshConsoleLog();return;}const reader=response.body.getReader();const decoder=new TextDecoder();let buffer="";while(true){const {done,value}=await reader.read();if(done)break;buffer+=decoder.decode(value,{stream:true});const parts=buffer.split("\\n\\n");buffer=parts.pop()||"";for(const part of parts){const dataLine=part.split("\\n").find(line=>line.startsWith("data: "));if(!dataLine)continue;let chunk;try{chunk=JSON.parse(dataLine.slice(6));}catch{continue;}if(chunk.error){streamTurn.text=(streamTurn.text||"")+"<strong>Stream error:</strong> "+esc(chunk.error);refreshConsoleLog();}else if(chunk.text){streamTurn.text=(streamTurn.text||"")+esc(chunk.text);refreshConsoleLog();}}}if(!streamTurn.text)streamTurn.text="<em>No governed model output — provider unavailable or authority denied.</em>";refreshConsoleLog();}catch(error){streamTurn.text=(streamTurn.text||"")+"<strong>Stream failed:</strong> "+esc(error.message);refreshConsoleLog();}}
async function submitConsoleMission(event){event.preventDefault();const input=$("console-input");const goal=input.value.trim();if(!goal)return;input.value="";consoleTurn("operator","<strong>"+esc(goal)+"</strong>");refreshConsoleLog();try{const result=await request(api.missions,{method:"POST",body:JSON.stringify({goal,actor:"console-user"})});consoleState.tracked={id:result.id,state:result.state,goal};consoleTurn("system","Mission "+esc(result.id)+" accepted ("+esc(result.state)+"). Live state follows via the event stream.",'<a href="#mission/'+encodeURIComponent(result.id)+'">Open mission detail</a>');refreshConsoleLog();}catch(error){consoleTurn("error","<strong>Rejected:</strong> "+esc(error.message));refreshConsoleLog();}}
function refreshConsoleLog(){const log=$("console-log");if(!log)return;log.innerHTML=consoleState.turns.length?consoleState.turns.map(turn=>consoleTurnHtml(turn)).join(""):'<p class="empty">No conversation yet.</p>';consoleScrollDown();}
function consoleScrollDown(){const log=$("console-log");if(log)log.scrollTop=log.scrollHeight;}
function consoleOnLiveEvent(type,payload){if(currentRoute!=="console")return;const tracked=consoleState.tracked;const relevant=tracked&&(String(payload?.missionId||payload?.taskId||"")===tracked.id||String(payload?.taskId||"")===tracked.loopId);if(type==="mission.started"&&relevant){consoleTurn("system","Mission started.");refreshConsoleLog();}else if(type==="mission.completed"&&relevant){consoleTurn("system","Mission completed and verified. "+(payload?.taskId?'<a href="#mission/'+encodeURIComponent(tracked.id)+'">Inspect the receipt</a>':""));tracked.state="COMPLETED";refreshConsoleLog();}else if(type==="mission.failed"&&relevant){consoleTurn("system","Mission failed. Open the detail trace for the failure evidence.");tracked.state="FAILED";refreshConsoleLog();}else if(type==="mission.cancelled"&&relevant){consoleTurn("system","Mission cancelled by operator decision.");tracked.state="CANCELLED";refreshConsoleLog();}else if((type==="tool.requested"||type==="tool.completed")&&relevant){consoleTurn("system",esc(type==="tool.requested"?"Tool requested":"Tool completed"));refreshConsoleLog();}}
async function renderMissions(){const data=await request(api.missions);const all=data.missions||[];const lanes=[["Active / queued",all.filter(m=>m.state==="RUNNING"||m.state==="QUEUED")],["Waiting approval",all.filter(m=>m.state==="FAILED"&&/awaiting|approval/i.test(m.error||""))],["Failed",all.filter(m=>m.state==="FAILED")],["Recently completed",all.filter(m=>m.state==="COMPLETED").slice(0,10)]];page.innerHTML='<section class="panel"><div class="panel-header"><div><h2>Mission Control</h2><p class="muted">Live lanes from durable mission state. Updates arrive over the event stream.</p></div><a class="button" href="#new-mission">New mission</a></div><div class="grid two section-gap">'+lanes.map(([label,items])=>'<section class="lane"><h3>'+esc(label)+' <span class="nav-count">'+items.length+'</span></h3>'+laneItems(items)+'</section>').join("")+'</div></section>';}
function laneItems(items){return items.length?'<div class="detail-list">'+items.map(m=>'<a class="lane-item" href="#mission/'+encodeURIComponent(m.id)+'"><strong>'+esc(m.goal)+'</strong><span>'+badge(m.state)+' <span class="muted">'+esc(fmt(m.updatedAt))+'</span></span></a>').join("")+'</div>':'<p class="empty">No missions in this lane.</p>';}
async function renderNewMission(){const data=await request(api.providers);const options=data.providers.filter(p=>p.available).map(p=>'<option value="'+esc(p.id)+'">'+esc(p.id)+'</option>').join("");page.innerHTML='<section class="panel"><h2>Plan before execution</h2><p class="muted">A dry run validates this request without starting an agent loop.</p><form id="mission-form" class="form-grid section-gap"><div class="field full"><label for="mission-goal">Outcome</label><textarea id="mission-goal" required placeholder="Inspect the repository and produce an evidence-backed architecture summary."></textarea></div><div class="field"><label for="mission-mode">Mode</label><select id="mission-mode"><option value="studio">Studio mission</option><option value="planning">Plan only</option><option value="research">Research</option><option value="coding">Coding</option></select></div><div class="field"><label for="mission-provider">Provider</label><select id="mission-provider"><option value="">Runtime default</option>'+options+'</select></div><div class="field"><label for="mission-model">Model override</label><input id="mission-model" placeholder="Configured default"></div><div class="field"><label for="mission-safety">Safety mode</label><select id="mission-safety"><option value="standard">Standard capability policy</option><option value="read-only">Read-only preference</option><option value="strict">Strict review preference</option></select></div><label class="checkbox field full"><input id="mission-dry-run" type="checkbox"> Validate only; do not execute</label><div class="actions field full"><button class="button" type="submit">Submit mission</button></div></form><div id="mission-result" class="muted section-gap"></div></section>';$("mission-form").addEventListener("submit",async event=>{event.preventDefault();try{const result=await request(api.missions,{method:"POST",body:JSON.stringify({goal:$("mission-goal").value,actor:"studio-user",mode:$("mission-mode").value,providerId:$("mission-provider").value||undefined,model:$("mission-model").value||undefined,safetyMode:$("mission-safety").value,dryRun:$("mission-dry-run").checked})});$("mission-result").innerHTML='Accepted: <a href="#mission/'+encodeURIComponent(result.id)+'">'+esc(result.id)+'</a> '+badge(result.state);notice("Mission request recorded.");}catch(error){notice(error.message,true);}});}
async function renderMission(id){if(!id)return renderMissions();const mission=await request(api.missions+"/"+encodeURIComponent(id));const detail=mission.detail||{};const tasks=detail.tasks||[];const task=tasks[0]||{};const capabilities=task.requiresPermissions||[];const cancellable=mission.state==="RUNNING"||mission.state==="QUEUED";page.innerHTML='<div class="grid two"><section class="panel"><h2>'+esc(mission.goal)+'</h2>'+row("Status",badge(mission.state),true)+row("Mission ID",mission.id)+row("Loop",mission.loopId||"Not started")+row("Provider",mission.providerId||"Runtime default")+row("Iterations",mission.iterations)+row("Capabilities",capabilities.length?capabilities.join(", "):"none reported")+row("Verification",detail.evaluation?.success?badge("PASSED"):detail.evaluation?badge("FAILED"):badge("PENDING"),true)+(mission.error?row("Error",mission.error):"")+'<div class="actions section-gap">'+(cancellable?'<button class="button danger" id="mission-cancel" data-id="'+esc(mission.id)+'">Cancel mission</button>':"")+'<button class="button secondary" id="mission-resume" data-id="'+esc(mission.id)+'">Resume</button>'+(mission.traceId?'<a class="button secondary" href="#trace/'+encodeURIComponent(mission.traceId)+'">View Trace</a>':"")+'<a class="button" href="#missions">All missions</a></div></section><section class="panel"><h2>Evidence</h2>'+recordList(detail.evidence||[])+'</section></div><section class="panel section-gap"><h2>Event timeline</h2>'+recordList(detail.events||[])+'</section>';$("mission-cancel")?.addEventListener("click",()=>missionAction(mission.id,"cancel","Cancel this running mission?"));$("mission-resume")?.addEventListener("click",()=>missionAction(mission.id,"resume","Resume this mission through the durable recovery path?"));}
async function missionAction(id,action,question){if(!window.confirm(question))return;try{const result=await request(api.missions+"/"+encodeURIComponent(id)+"/"+action,{method:"POST",body:JSON.stringify({})});notice(action==="cancel"?"Cancellation requested. The mission record reflects the terminal state shortly.":"Resume dispatched through the durable recovery path.");renderMission(id);}catch(error){notice(error.message,true);}}
function recordList(items){return items.length?'<div class="detail-list">'+items.slice(0,100).map(item=>row(item.status||item.type||"record",item.summary||item.content||item.reason||item.name||item.id||JSON.stringify(item))).join("")+'</div>':'<p class="empty">No records reported.</p>';}
async function renderAgents(){const [registry,state]=await Promise.all([request(api.agents),request(api.dashboardState)]);const agents=registry.agents||[];const dashAgents=(state.agents&&state.agents.agents)||[];const evaluations=(state.harness&&state.harness.evaluations)||[];const dims=evaluations.filter(e=>e.result?.dimensions).map(e=>e.result.dimensions);const avg=(key)=>dims.length?Math.round(dims.reduce((t,d)=>t+(Number(d[key])||0),0)/dims.length):null;const rows=agents.map(a=>{const dash=dashAgents.find(d=>d.id===a.identity.id);const assigned=dash?.assignedMissions?.length||0;return '<tr><td><strong>'+esc(a.identity.name)+'</strong><div class="muted mono">'+esc(a.identity.id)+'</div><div class="muted">'+esc(a.identity.description)+'</div></td><td>'+badge(a.trustLevel)+'</td><td>'+esc(a.capabilities.join(", ")||"—")+'</td><td>'+esc(a.skills.join(", ")||"—")+'</td><td>'+esc(a.specialization.join(", ")||"—")+'</td><td>'+badge(assigned>0?"ASSIGNED":"AVAILABLE")+(assigned?' <span class="muted">'+esc(assigned)+' mission(s)</span>':'')+'</td></tr>';});const summary=dims.length?'<div class="grid cards section-gap">'+card("Capability discipline",avg("capabilityDiscipline"),"Across "+evaluations.length+" stored evaluation(s)")+card("Recovery",avg("recovery"),"Durable eval history")+card("Planning",avg("planning"),"")+card("Evidence quality",avg("evidenceQuality"),"")+'</div>':'';page.innerHTML='<section class="panel"><div class="panel-header"><div><h2>Agent workspace</h2><p class="muted">Registered specialists, their capabilities, and current assignment state. This is registry state, not a claim that an agent is currently executing.</p></div></div>'+table(["Agent","Trust","Capabilities","Skills","Specialization","Work"],rows)+'</section>'+(summary||'<p class="empty section-gap">No stored evaluation dimensions yet — run and evaluate missions to see agent-quality summaries.</p>');}
async function renderModels(){const data=await request(api.providers);page.innerHTML='<section class="panel"><h2>Provider Control Center</h2><p class="muted">L0 means registered. L1 requires a successful health check. Live inference certification is never inferred from configuration.</p>'+table(["Provider","Models","Reachability","Certification","Action"],data.providers.map(p=>'<tr><td><strong>'+esc(p.id)+'</strong></td><td>'+esc((p.capabilities?.models||[]).join(", ")||"Not reported")+'</td><td>'+badge(p.available?"AVAILABLE":"UNAVAILABLE")+'</td><td>'+badge(p.available?"L1 HEALTH":"L0 ONLY")+'</td><td><button class="button secondary" data-provider-test="'+esc(p.id)+'">Safe health test</button></td></tr>'))+'<p class="muted">Fallback order: '+esc(data.fallbackOrder.join(" → ")||"none")+'</p></section>';bindProviderTests();}
function bindProviderTests(){document.querySelectorAll("[data-provider-test]").forEach(button=>button.addEventListener("click",async()=>{try{const result=await request(api.providers+"/test",{method:"POST",body:JSON.stringify({providerId:button.dataset.providerTest})});notice(result.message||result.status||"Health check completed.");}catch(error){notice(error.message,true);}}));}
async function renderActions(){const data=await request(api.actions);const descriptors=data.providers.flatMap(p=>p.actions.map(a=>({provider:p.metadata.providerId,health:p.health.status,...a})));page.innerHTML='<div class="grid two"><section class="panel"><h2>Action providers</h2>'+table(["Provider","Boundary","Health","Actions"],data.providers.map(p=>'<tr><td>'+esc(p.metadata.displayName)+'</td><td>'+esc(p.metadata.boundary)+'</td><td>'+badge(p.health.status)+'</td><td>'+esc(p.actions.length)+'</td></tr>'))+'</section><section class="panel"><h2>Ambiguous external state</h2><p class="muted">These records require reconciliation. QUACK will not blindly repeat them.</p>'+recordList(data.ambiguous||[])+'</section></div><section class="panel section-gap"><h2>Capability catalog</h2>'+table(["Action","Provider","Risk","Approval"],descriptors.map(a=>'<tr><td><strong>'+esc(a.name)+'</strong><div class="muted">'+esc(a.description)+'</div></td><td>'+esc(a.provider)+'</td><td>'+badge(a.riskClass)+'</td><td>'+badge(a.approval)+'</td></tr>'))+'</section><section class="panel section-gap"><h2>Execution ledger</h2>'+table(["Execution","Action","State","Updated"],(data.executions||[]).map(x=>'<tr><td class="mono">'+esc(x.executionId)+'</td><td>'+esc(x.actionName)+'</td><td>'+badge(x.state)+'</td><td>'+esc(fmt(x.updatedAt))+'</td></tr>'))+'</section>';}
async function renderIntegrations(){const data=await request(api.mcp);page.innerHTML='<section class="panel"><h2>MCP integrations</h2><p class="muted">Supported transports: '+esc(data.supportedTransports.join(", "))+'. Legacy SSE: '+esc(data.legacySse)+'.</p>'+table(["Server","Transport","Boundary","Enabled","Health"],data.servers.map(s=>'<tr><td>'+esc(s.metadata.displayName)+'</td><td>'+esc(s.metadata.transport)+'</td><td>'+esc(s.metadata.boundary)+'</td><td>'+badge(s.enabled?"ENABLED":"DISABLED")+'</td><td>'+badge(s.health.status)+'</td></tr>'))+'<p class="callout section-gap">Stdio receives a minimal environment. Secret-like variables require explicit grants. Isolated mode requires a declared OS sandbox launcher.</p></section>';}
async function renderBrowser(){const data=await request(api.actions);const browser=data.providers.find(p=>p.metadata.providerId==="browser.playwright");page.innerHTML='<section class="panel"><h2>Browser runtime</h2>'+row("Status",badge(browser?.health?.status||"UNAVAILABLE"),true)+row("Boundary",browser?.metadata?.boundary||"local")+row("Discovered actions",browser?.actions?.length||0)+row("Default network",badge("DENY"),true)+'<p class="callout section-gap">Page content is untrusted data. It cannot grant permissions, change policy, or approve an action.</p></section>';}
async function renderFiles(){const data=await request(api.settings);page.innerHTML='<section class="panel"><h2>Workspace boundary</h2>'+row("File access",badge("POLICY GATED"),true)+row("Granted permissions",(data.safety.permissions||[]).join(", ")||"none")+row("Browser uploads",badge("APPROVAL REQUIRED"),true)+'<p class="muted section-gap">The browser has no arbitrary file picker. Upload actions accept only paths contained by configured workspace roots. File mutations remain action-policy decisions.</p></section>';}
async function renderApprovals(){const [queueResult,actions,proposals]=await Promise.all([request(api.approvals).catch(error=>({unavailable:error.message})),request(api.actions),request(api.proposals)]);const waiting=(actions.executions||[]).filter(x=>x.state==="AWAITING_APPROVAL");const proposed=(proposals.proposals||[]).filter(x=>x.status==="PROPOSED");const queue=queueResult.approvals||[];const queued=queue.filter(x=>x.state==="PENDING");page.innerHTML='<section class="panel"><div class="panel-header"><div><h2>Approval queue</h2><p class="muted">Human decisions waiting on this operator through the canonical approval callback.</p></div></div>'+(queueResult.unavailable?'<p class="empty">'+esc(queueResult.unavailable)+'</p>':table(["Request","Context","Expires","Decision"],queued.map(x=>'<tr><td>'+esc(x.prompt)+'<div class="muted mono">'+esc(x.id)+'</div></td><td class="mono">'+esc(JSON.stringify(x.context||{}).slice(0,80))+'</td><td>'+esc(fmt(x.expiresAt))+'</td><td><button class="button secondary" data-queue-approve="'+esc(x.id)+'">Approve</button> <button class="button danger" data-queue-deny="'+esc(x.id)+'">Deny</button></td></tr>')))+(queued.length?'<p class="callout">Expiry denies by default. Decisions go through the same capability path as the CLI — this screen never grants authority directly.</p>':'')+'</section><section class="panel section-gap"><h2>Action approvals</h2>'+table(["Execution","Action","Risk","Updated"],waiting.map(x=>'<tr><td class="mono">'+esc(x.executionId)+'</td><td>'+esc(x.actionName)+'</td><td>'+badge(x.riskLevel)+'</td><td>'+esc(fmt(x.updatedAt))+'</td></tr>'))+'</section><section class="panel section-gap"><h2>Improvement decisions</h2><p class="muted">These decisions do not mutate source or merge code.</p>'+table(["Proposal","Risk","Decision"],proposed.map(p=>'<tr><td>'+esc(p.proposal?.description||p.reason)+'</td><td>'+badge(p.risk)+'</td><td><button class="button secondary" data-proposal-action="approve" data-id="'+esc(p.id)+'">Approve</button> <button class="button danger" data-proposal-action="reject" data-id="'+esc(p.id)+'">Reject</button></td></tr>'))+'</section>';document.querySelectorAll("[data-proposal-action]").forEach(button=>button.addEventListener("click",()=>decideProposal(button.dataset.id,button.dataset.proposalAction)));document.querySelectorAll("[data-queue-approve],[data-queue-deny]").forEach(button=>button.addEventListener("click",()=>decideQueuedApproval(button.dataset.queueApprove||button.dataset.queueDeny,button.dataset.queueApprove?"approve":"deny")));}
async function decideQueuedApproval(id,action){if(!window.confirm("Record this human decision?"))return;try{await request(api.approvals+"/"+encodeURIComponent(id)+"/"+action,{method:"POST",body:JSON.stringify({actor:"studio-user",reason:"Decision confirmed in QUACK Control Room."})});notice("Decision recorded through the canonical approval path.");}catch(error){notice(error.message,true);}renderApprovals();}
async function decideProposal(id,action){if(!window.confirm("Record this human decision? This does not mutate source files."))return;try{await request(api.proposals+"/"+encodeURIComponent(id)+"/"+action,{method:"POST",body:JSON.stringify({confirm:true,actor:"studio-user",reason:"Decision confirmed in QUACK Control Room."})});notice("Decision recorded. No source mutation occurred.");renderApprovals();}catch(error){notice(error.message,true);}}
const traceFilters={text:"",type:"all"};
async function renderTraces(){const state=await request(api.dashboardState);const traces=(state.harness&&state.harness.traces)||[];const rows=traces.slice().reverse().map(t=>'<tr><td><a href="#trace/'+encodeURIComponent(t.id)+'">'+esc(t.missionInput.goal)+'</a><div class="muted mono">'+esc(t.id)+'</div></td><td>'+badge(t.finalOutcome.success?"COMPLETED":t.finalOutcome.state)+'</td><td>'+esc((t.events||[]).length)+'</td><td>'+esc((t.toolsExecuted||[]).length)+'</td><td>'+esc(t.finalOutcome.latencyMs+"ms")+'</td><td>'+esc(fmt(t.completedAt))+'</td></tr>');page.innerHTML='<section class="panel"><div class="panel-header"><div><h2>Trace Center</h2><p class="muted">Mission execution timelines from the durable trace repository. Real records only — no fabricated entries.</p></div></div>'+(traces.length?table(["Mission","Outcome","Events","Tool calls","Latency","Completed"],rows):'<p class="empty">No traces stored yet. Run and complete missions to populate this index.</p>')+'</section>';}
async function renderTraceDetail(id){if(!id)return renderTraces();let trace=null;try{trace=await request(api.missions.replace(/missions$/,"traces")+"/"+encodeURIComponent(id));}catch{const state=await request(api.dashboardState);trace=((state.harness&&state.harness.traces)||[]).find(t=>t.id===id);}
if(!trace){page.innerHTML='<section class="panel"><h2>Trace detail</h2><p class="empty">Trace '+esc(id)+' was not found in the repository.</p></section>';return;}
const events=(trace.events||[]);const caps=(trace.capabilitiesRequested||[]);const tools=(trace.toolsExecuted||[]);const verifications=(trace.verificationResults||[]);const iterations=(trace.iterations||[]);const plans=(trace.plansGenerated||[]);const outcome=trace.finalOutcome||{};const instruction=(trace.instruction||[]);
// One chronological timeline: trace events are real records; nothing synthesized.
const timeline=events.map(e=>({at:e.timestamp,type:e.type,actor:e.actor,taskId:e.taskId,summary:eventSummary(e)})).filter(x=>x.summary);
const typeFilter='<div class="field"><label for="trace-type-filter">Event type</label><select id="trace-type-filter"><option value="all">All types</option>'+[...new Set(timeline.map(x=>x.type))].map(t=>'<option value="'+esc(t)+'"'+(traceFilters.type===t?" selected":"")+'>'+esc(t)+'</option>').join("")+'</select></div>';
const filtered=timeline.filter(x=>(traceFilters.type==="all"||x.type===traceFilters.type)&&(!traceFilters.text||JSON.stringify(x).toLowerCase().includes(traceFilters.text)));
page.innerHTML='<section class="panel"><div class="panel-header"><div><h2>'+esc(trace.missionInput.goal)+'</h2><p class="muted mono">'+esc(trace.id)+(trace.missionInput.missionId?' · mission '+esc(trace.missionInput.missionId):'')+'</p></div><span class="badge '+(outcome.success?"ok":"danger")+'">'+esc(outcome.state||"UNKNOWN")+'</span></div></section>'+
'<div class="grid two section-gap"><section class="panel"><h2>Overview</h2>'+row("Outcome",badge(outcome.success?"COMPLETED":(outcome.state||"FAILED")),true)+row("Latency",outcome.latencyMs+"ms")+row("Plans",plans.length)+row("Iterations",iterations.length)+row("Capability checks",caps.length)+row("Tool calls",tools.length)+'<div class="actions section-gap"><a class="button secondary" href="#mission/'+encodeURIComponent(trace.missionInput.missionId||trace.id)+'">Mission record</a></div></section>'+
'<section class="panel"><h2>Capabilities</h2>'+(caps.length?table(["Capability","Decision","Reason"],caps.map(c=>'<tr><td class="mono">'+esc(c.capability)+'</td><td>'+badge(c.decision||c.eventType)+'</td><td class="muted">'+esc(c.reason||"—")+'</td></tr>')):'<p class="empty">No capability records in this trace.</p>')+'</section>'+
'<section class="panel"><h2>Tool activity</h2>'+(tools.length?table(["Tool","Result","Detail"],tools.map(t=>'<tr><td class="mono">'+esc(t.toolId)+'</td><td>'+badge(t.success?"SUCCEEDED":"FAILED")+'</td><td class="muted mono">'+esc(JSON.stringify(t.output||t.error||{}).slice(0,120))+'</td></tr>')):'<p class="empty">No tool executions recorded.</p>')+'</section>'+
'<section class="panel"><h2>Verification</h2>'+(verifications.length?table(["Iteration","Result","Reason"],verifications.map(v=>'<tr><td>'+esc(v.iteration)+'</td><td>'+badge(v.success?"VERIFIED":"FAILED")+'</td><td class="muted">'+esc(v.reason)+'</td></tr>')):'<p class="empty">No verification records in this trace.</p>')+'</section>'+
'<section class="panel"><h2>Governed instructions</h2><p class="muted">Metadata-only dispatch records (P8.6): outcome, instruction identity, trust census, budget, defense flags. Instruction content is never stored or rendered here.</p>'+(instruction.length?table(["Digest","Outcome","Layers","Items","Omitted","Flags","Dispatched"],instruction.map(r=>'<tr><td class="mono" title="'+esc(r.digest)+'">'+esc(String(r.digest||"").slice(0,12))+'…</td><td>'+badge(r.outcome)+(r.errorCode?'<div class="muted mono">'+esc(r.errorCode)+'</div>':"")+'</td><td>'+esc((r.layerCensus||[]).map(c=>c.layer+" ("+c.itemCount+")").join(", ")||"—")+'</td><td>'+esc(r.totalItems)+'</td><td>'+esc(r.omittedItemCount)+'</td><td>'+esc(r.injectionFlagCount)+'</td><td>'+esc(fmt(r.dispatchedAt))+'</td></tr>')):'<p class="empty">No governed-instruction dispatches recorded for this trace. Missions that used no composed instructions report nothing here.</p>')+'</section>'+
'<section class="panel"><h2>Evidence chain</h2>'+(trace.receipt?'<p class="callout">This mission completed with a durable receipt — see the mission record.</p>':'<p class="empty">No receipt recorded for this execution.</p>')+'</section>'+
'<section class="panel"><h2>Receipt</h2>'+row("Final state",badge(outcome.state||"UNKNOWN"),true)+row("Error",outcome.error||"—")+'</section></div>'+
'<section class="panel section-gap"><div class="panel-header"><div><h2>Timeline</h2><p class="muted">Chronological execution events from the trace repository — filtered, not synthesized.</p></div><div class="trace-filters">'+typeFilter+'</div></div><div class="field"><label for="trace-text-filter">Filter text</label><input id="trace-text-filter" value="'+esc(traceFilters.text)+'" placeholder="actor, task, payload…"></div><div class="detail-list section-gap" id="trace-timeline">'+(filtered.length?filtered.map(x=>'<article class="trace-item"><span class="badge">'+esc(x.type)+'</span><span class="muted">'+esc(fmt(x.at))+(x.taskId?' · '+esc(x.taskId):'')+' · '+esc(x.actor)+'</span><div class="mono muted">'+esc(x.summary)+'</div></article>').join(""):'<p class="empty">No events match the current filters.</p>')+'</div></section>';
$("trace-type-filter").addEventListener("change",e=>{traceFilters.type=e.target.value;renderTraceDetail(id);});$("trace-text-filter").addEventListener("input",e=>{traceFilters.text=e.target.value.trim().toLowerCase();const list=$("trace-timeline");if(!list)return;const re=filtered;list.innerHTML=re.length?re.map(x=>'<article class="trace-item"><span class="badge">'+esc(x.type)+'</span><span class="muted">'+esc(fmt(x.at))+(x.taskId?' · '+esc(x.taskId):'')+' · '+esc(x.actor)+'</span><div class="mono muted">'+esc(x.summary)+'</div></article>').join(""):'<p class="empty">No events match the current filters.</p>';});}
function eventSummary(e){const p=e.payload||{};const keys=Object.keys(p);if(!keys.length)return "";const skip=new Set(["id","timestamp","taskId","missionId","executionId"]);const parts=[];for(const k of keys.slice(0,6)){if(skip.has(k))continue;const v=p[k];parts.push(k+": "+esc(typeof v==="string"?v:JSON.stringify(v)).slice(0,80));}return parts.join(" · ");}
async function renderArtifacts(){const state=await request(api.dashboardState);const traces=(state.harness&&state.harness.traces)||[];const rows=[];for(const t of traces){for(const iteration of (t.iterations||[])){for(const call of (iteration.toolCalls||[])){if(call.success&&call.output&&Object.keys(call.output).length){rows.push('<tr><td>'+esc(t.missionInput.goal)+'<div class="muted mono">'+esc(t.id)+'</div></td><td class="mono">'+esc(call.toolId)+'</td><td>'+badge("TOOL OUTPUT")+'</td><td class="mono muted">'+esc(JSON.stringify(call.output).slice(0,120))+'</td></tr>');}}}}page.innerHTML='<section class="panel"><div class="panel-header"><div><h2>Artifacts</h2><p class="muted">Evidence- and workspace-backed outputs only. An artifact is presented with its source mission and producing tool — there is no separate artifact store.</p></div></div>'+(rows.length?table(["Source mission","Producing tool","Kind","Output"],rows):'<p class="empty">No artifact-backed outputs recorded yet. Artifacts appear when missions produce verified tool output.</p>')+'</section>';}
async function renderAudit(){const data=await request(api.audit+"?limit=100");const records=data.records||[];page.innerHTML='<section class="panel"><div class="panel-header"><div><h2>Audit</h2><p class="muted">'+esc(data.total||0)+' governance records. Security/accountability record — distinct from trace observability. Payloads are redacted at this boundary.</p></div></div>'+(records.length?table(["Time","Type","Actor","Payload"],records.map(r=>'<tr><td>'+esc(fmt(r.timestamp))+'</td><td class="mono">'+esc(r.type)+'</td><td>'+esc(r.actor)+'</td><td class="mono muted">'+esc(eventSummary(r))+'</td></tr>')):'<p class="empty">No audit records yet.</p>')+'</section>';}
async function renderEvidence(){const [data,state]=await Promise.all([request(api.memory),request(api.dashboardState)]);const evaluations=(state.harness&&state.harness.evaluations)||[];const instruction=(state.harness&&state.harness.instruction)||null;const evalRows=evaluations.slice().reverse().map(e=>'<tr><td class="mono">'+esc(e.id||e.traceId)+'</td><td class="mono">'+esc(e.missionId||"—")+'</td><td>'+(e.result?.score!==undefined?badge(e.result.score>=80?"PASS":e.result.score>=50?"PARTIAL":"FAIL")+" "+esc(e.result.score):"—")+'</td><td>'+(e.result?.dimensions?'<span class="badge ok">C'+esc(e.result.dimensions.capabilityDiscipline)+'</span> <span class="badge ok">R'+esc(e.result.dimensions.recovery)+'</span> <span class="badge warn">P'+esc(e.result.dimensions.planning)+'</span> <span class="badge">E'+esc(e.result.dimensions.evidenceQuality)+'</span>'+(e.result.dimensions.instruction?' <span class="badge">I'+esc(e.result.dimensions.instruction.instructionIntegrity)+'</span>':"")+(e.result.dimensions.memory?' <span class="badge">M'+esc(e.result.dimensions.memory.scopeCorrectness)+'</span>':""):'—')+'</td><td>'+esc(e.createdAt?fmt(e.createdAt):"—")+'</td></tr>');const instructionPanel=instruction?'<section class="panel"><h2>Instruction telemetry</h2><p class="muted">Governed-instruction dispatch census aggregated from trace records — identity and counts only, never content.</p><div class="grid cards section-gap">'+card("Dispatches",instruction.dispatchCount,"Across "+instruction.missionCount+" mission(s)")+card("Dispatched",instruction.dispatchedCount,"Reached the governed runtime")+card("Rejected",instruction.rejectedCount,"Failed closed at defense/adaptation")+card("Injection flags",instruction.injectionFlagCount,"Defense-in-depth detections (metadata)")+'</div>'+(instruction.deniedCount||instruction.providerErrorCount?'<div class="detail-list section-gap">'+row("Capability denials",instruction.deniedCount)+row("Provider errors",instruction.providerErrorCount)+'</div>':"")+'</section>':"";page.innerHTML='<section class="panel"><div class="panel-header"><div><h2>Evaluation history</h2><p class="muted">Stored MissionEvaluator results — score plus capability discipline (C), recovery (R), planning (P), evidence quality (E) dimensions; instruction (I) appears for missions with governed instruction dispatches.</p></div></div>'+(evalRows.length?table(["Evaluation","Mission","Score","Dimensions","Created"],evalRows):'<p class="empty">No stored evaluations yet. Run a mission and evaluate it to populate this history.</p>')+'</section>'+(instructionPanel||'<section class="panel"><h2>Instruction telemetry</h2><p class="empty">No governed-instruction dispatches recorded yet. Instruction telemetry appears when missions dispatch composed instructions through the governed runtime.</p></section>')+'<div class="grid two section-gap"><section class="panel"><h2>Mission memory</h2>'+recordList(data.mission||[])+'</section><section class="panel"><h2>Evidence memory</h2>'+recordList(data.evidence||[])+'</section><section class="panel"><h2>Decisions</h2>'+recordList(data.decisions||[])+'</section><section class="panel"><h2>Failures</h2>'+recordList(data.failures||[])+'</section></div>'+renderSemanticMemoryPanel(data);}
function renderSemanticMemoryPanel(data){const semantic=data.semantic;if(!semantic||!Array.isArray(semantic.records))return '<section class="panel section-gap"><h2>Semantic memory</h2><p class="empty">Semantic memory is not available in this composition.</p></section>';const stats=semantic.stats||{};const records=semantic.records;const rows=records.map(r=>'<tr><td class="mono" title="'+esc(r.memoryId)+'">'+esc(String(r.memoryId||"").slice(0,18))+'…</td><td>'+badge(r.scope)+'</td><td>'+esc(r.owner)+'</td><td>'+esc(r.provenance?.sourceKind||"—")+'</td><td>'+(r.embedding?badge("EMBEDDED"):badge("PLAIN"))+'</td><td>'+esc(r.lifecycle)+'</td><td>'+esc(fmt(r.createdAt))+'</td></tr>');return '<section class="panel section-gap"><div class="panel-header"><div><h2>Semantic memory</h2><p class="muted">Governed semantic records (P9): scope, owner, provenance, embedding state, lifecycle. Content stays bounded to a redacted preview; memory is data, never authority.</p></div></div><div class="grid cards">'+card("Records",stats.recordCount??"0","Canonical store")+(stats.embeddingsEnabled?card("Embeddings",badge("ENABLED",true),"Governed model path"):card("Embeddings",badge("DISABLED"),"No embedding provider"))+(stats.index?card("Indexed chunks",stats.index.chunkCount??0,"Derived index"):card("Indexed chunks",0,"Derived index"))+'</div>'+(records.length?table(["Memory","Scope","Owner","Source","Embedding","Lifecycle","Created"],rows):'<p class="empty">No semantic memory stored yet. Records appear when an authorized actor explicitly remembers content.</p>')+'</section>';}
async function renderEcosystem(){const data=await request(api.extensions);const extensions=data.extensions||[];const rows=extensions.map(x=>'<tr><td class="mono">'+esc(x.id)+'@'+esc(x.version)+'</td><td>'+esc(x.kind)+'</td><td>'+badge(x.lifecycle)+'</td><td>'+esc(x.signatureState)+'</td><td>'+(x.integrityState==="MATCHED"?badge("MATCHED",true):badge("MISMATCHED"))+'</td><td class="mono muted">'+esc((x.declaredCapabilities||[]).join(", ").slice(0,60)||"—")+'</td><td class="mono muted">'+esc(String(x.dependencyCount??0))+'</td></tr>');page.innerHTML='<section class="panel"><div class="panel-header"><div><h2>Ecosystem</h2><p class="muted">Governed extension catalog (P10): manifest, integrity, lifecycle, declared capabilities — metadata only, never package content. Declared capabilities grant nothing; package content is never executed by the catalog.</p></div></div><div class="grid cards">'+card("Extensions",extensions.length,"Registered packages")+'</div>'+(extensions.length?table(["Extension","Kind","Lifecycle","Signature","Integrity","Declared capabilities","Deps"],rows):'<p class="empty">No extensions installed yet. Packages appear when an authorized actor installs one explicitly.</p>')+'</section>';}
async function renderSystem(){const data=await request(api.systemStatus);page.innerHTML='<div class="grid cards">'+card("NVIDIA NIM",data.setup.nvidia,"Secret presence only")+card("Ollama",data.setup.ollama,"Local service")+card("Browser",data.setup.browser,"Installed executable")+card("MCP servers",data.mcp.configured,"Configured now")+'</div><div class="grid two section-gap"><section class="panel"><h2>API security</h2>'+row("Loopback only",badge(data.safety.loopbackOnly?"ENABLED":"DISABLED"),true)+row("Authentication",badge(data.safety.authentication?"ENABLED":"DISABLED"),true)+row("CSRF protection",badge(data.safety.csrfProtection?"ENABLED":"DISABLED"),true)+row("Outbound default",badge(data.safety.networkDefault),true)+'</section><section class="panel"><h2>Action recovery</h2>'+row("Ledger records",data.actions.total)+row("Awaiting approval",data.actions.awaitingApproval)+row("Ambiguous",data.actions.ambiguous)+'</section></div>';}
async function renderSettings(){const data=await request(api.settings);page.innerHTML='<section class="panel"><h2>Safe runtime settings</h2><p class="muted">Hard approval and isolation gates cannot be weakened here.</p><form id="settings-form" class="form-grid section-gap"><label class="checkbox"><input id="setting-improvement" type="checkbox" '+(data.improvement.enabled?"checked":"")+'> Enable improvement evaluation</label><label class="checkbox"><input id="setting-auto" type="checkbox" '+(data.improvement.autoEvaluate?"checked":"")+'> Evaluate eligible missions automatically</label><div class="field"><label for="setting-evidence">Minimum evidence</label><input id="setting-evidence" type="number" min="0" value="'+esc(data.improvement.minimumEvidence)+'"></div><div class="field"><label for="setting-cooldown">Cooldown (ms)</label><input id="setting-cooldown" type="number" min="0" value="'+esc(data.improvement.cooldownMs)+'"></div><div class="field full"><label for="setting-approval">Self-modification approval</label><input id="setting-approval" value="Required — cannot be disabled" disabled></div><div class="actions field full"><button class="button" type="submit">Save settings</button></div></form></section>';$("settings-form").addEventListener("submit",async event=>{event.preventDefault();try{await request(api.settings,{method:"PATCH",body:JSON.stringify({improvement:{enabled:$("setting-improvement").checked,autoEvaluate:$("setting-auto").checked,minimumEvidence:Number($("setting-evidence").value),cooldownMs:Number($("setting-cooldown").value)}})});notice("Safe runtime settings updated.");}catch(error){notice(error.message,true);}});}
function applyTheme(theme){document.documentElement.dataset.theme=theme;localStorage.setItem("quack-theme",theme);$("theme-toggle").setAttribute("aria-label","Switch to "+(theme==="dark"?"light":"dark")+" theme");}
$("theme-toggle").addEventListener("click",()=>applyTheme(document.documentElement.dataset.theme==="dark"?"light":"dark"));applyTheme(localStorage.getItem("quack-theme")||"dark");
async function bootstrap(){try{const status=await request(api.systemStatus);$("runtime-dot").className="status-dot ok";$("runtime-label").textContent="Runtime connected";$("approval-count").textContent=String(status.actions.awaitingApproval);if(!localStorage.getItem("quack-setup-complete")){const checks=[["Runtime",status.runtime.status||"HEALTHY"],["NVIDIA NIM",status.setup.nvidia],["Ollama",status.setup.ollama],["Browser",status.setup.browser],["Network policy","DENY BY DEFAULT"]];$("setup-checks").innerHTML=checks.map(x=>'<div class="check-row"><strong>'+esc(x[0])+'</strong><span>'+badge(x[1])+'</span></div>').join("");$("setup-wizard").hidden=false;$("setup-finish").focus();}}catch{$("runtime-dot").className="status-dot bad";$("runtime-label").textContent="Runtime unavailable";}}
function connectLive(){let source;try{source=new EventSource(api.events);}catch{return;}source.onopen=()=>{$("runtime-dot").className="status-dot ok";$("runtime-label").textContent="Runtime connected";consoleState.connected=true;const badgeEl=$("console-stream-badge");if(badgeEl){badgeEl.className="badge ok";badgeEl.textContent="STREAM LIVE";}const hint=$("console-hint");if(hint)hint.hidden=true;};source.onerror=()=>{$("runtime-dot").className="status-dot warn";$("runtime-label").textContent="Event stream reconnecting";consoleState.connected=false;const badgeEl=$("console-stream-badge");if(badgeEl){badgeEl.className="badge warn";badgeEl.textContent="STREAM OFFLINE";}const hint=$("console-hint");if(hint)hint.hidden=false;};const liveTypes=["task.created","task.started","task.completed","task.failed","mission.started","mission.resumed","mission.completed","mission.failed","mission.cancelled","mission.recovery_started","mission.recovery_completed","approval.requested","approval.decided","workflow.completed","workflow.failed","node.completed","node.failed","tool.requested","tool.completed","instruction.dispatched","instruction.rejected","memory.admitted","memory.rejected","memory.persisted","memory.embedding.completed","memory.embedding.failed","memory.indexed","memory.retrieved","memory.deleted","memory.compacted","extension.discovered","extension.validated","extension.admitted","extension.installed","extension.enabled","extension.disabled","extension.quarantined","extension.removed","extension.rejected"];let pending=null;const refresh=(event)=>{try{const payload=event?JSON.parse(event.data):null;if(event)consoleOnLiveEvent(event.type||"unknown",payload?.payload||payload);}catch{}clearTimeout(pending);pending=setTimeout(()=>{refreshApprovalCount();if(["missions","mission","overview","approvals","evidence","trace","ecosystem"].includes(currentRoute))render(location.hash.split("/")[1]).catch(()=>{});},150);};liveTypes.forEach(type=>source.addEventListener(type,refresh));}
async function refreshApprovalCount(){try{const queue=await request(api.approvals).catch(()=>null);if(queue&&Array.isArray(queue.approvals)){$("approval-count").textContent=String(queue.approvals.filter(x=>x.state==="PENDING").length);return;}}catch{}try{const status=await request(api.systemStatus);$("approval-count").textContent=String(status.actions.awaitingApproval);}catch{}}
$("setup-finish").addEventListener("click",()=>{localStorage.setItem("quack-setup-complete","1");$("setup-wizard").hidden=true;$("page-title").focus();});window.addEventListener("hashchange",route);route();bootstrap();connectLive();`;
}
